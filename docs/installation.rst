============
Installation
============

At the command line::

    $ easy_install hivemind_contrib

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv hivemind_contrib
    $ pip install hivemind_contrib
